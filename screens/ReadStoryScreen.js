import * as React from "react";
import {
  View,
  Text,
  TextInput,
  StyleSheet,
  TouchableOpacity,
} from "react-native";
import read from "../assets/read.png";

export default class ReadStoryScreen extends React.Component {
  render() {
    return (
      <View>
        <Text style={{ textAlign: "center" }}>Read Story Screen</Text>
      </View>
    );
  }
}
